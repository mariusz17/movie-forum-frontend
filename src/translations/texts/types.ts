export type Texts = {
  appName: string;
  welcomeHeading: string;
  welcomeText: string;
  forum: string;
  search: string;
  searchPlaceholder: string;
  favourites: string;
};

export type TextsKeys = keyof Texts;
